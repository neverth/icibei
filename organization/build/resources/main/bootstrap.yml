server:
  port: ${SERVER_PORT:8010}

spring:
  application:
    name: icibei-organization
  cloud:
    nacos:
      discovery:
        server-addr: ${REGISTER_HOST:106.14.44.108}:${REGISTER_PORT:8848}
      config:
        server-addr: ${REGISTER_HOST:106.14.44.108}:${REGISTER_PORT:8848}
        file-extension: yml
    sentinel:
      transport:
        dashboard: ${SENTINEL_DASHBOARD_HOST:106.14.44.108}:${SENTINEL_DASHBOARD_PORT:8021}
